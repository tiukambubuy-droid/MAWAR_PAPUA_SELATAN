import assert from "node:assert/strict";import test from "node:test";import ts from "typescript";import{readFile}from"node:fs/promises";
const source=await readFile("lib/monitoring-presentation.ts","utf8"),output=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.ESNext,target:ts.ScriptTarget.ES2022}}).outputText,format=await import(`data:text/javascript;base64,${Buffer.from(output).toString("base64")}`);
test("monitoring presentation formatter is deterministic and Indonesian",()=>{assert.equal(format.formatMonitoringTimestamp("2026-07-24T22:42:00+09:00"),"24 Juli 2026 · 22.42 WIT");assert.equal(format.formatMonitoringDateRange("2026-07-23","2026-08-15"),"23 Jul–15 Agu 2026");assert.equal(format.formatMonitoringSeason("MT1-2026"),"MT I 2026");assert.equal(format.formatMonitoringSeason("MT2-2026"),"MT II 2026")});
test("monitoring status and percentage presentation preserve source values",()=>{const source={status:"approved",value:83.333};assert.equal(format.formatMonitoringStatus(source.status),"Disetujui");assert.equal(format.formatMonitoringStatus("pending"),"Menunggu");assert.equal(format.formatMonitoringStatus("monitored"),"Terpantau");assert.equal(format.formatMonitoringStatus("not_monitored"),"Belum dipantau");assert.equal(format.formatMonitoringStatus("prototype"),"Prototipe");assert.equal(format.formatMonitoringStatus("simulation"),"Simulasi");assert.equal(format.formatMonitoringPercent(source.value),"83,3%");assert.deepEqual(source,{status:"approved",value:83.333})});
test("monitoring formatter handles null and invalid values without NaN or Invalid Date",()=>{for(const value of[null,undefined,"invalid",NaN,Infinity]){assert.equal(format.formatMonitoringTimestamp(value),"Belum tersedia");assert.equal(format.formatMonitoringPercent(value),"Belum tersedia")}assert.equal(format.formatMonitoringDateRange(null,"2026-08-15"),"Belum tersedia")});
