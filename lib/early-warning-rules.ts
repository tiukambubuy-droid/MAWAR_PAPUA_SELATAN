export const riskThresholds=[{min:1,max:5,level:"Rendah"},{min:6,max:10,level:"Sedang"},{min:11,max:15,level:"Tinggi"},{min:16,max:25,level:"Kritis"}] as const;
export function riskLevel(likelihood:number|null,impact:number|null){if(likelihood===null||impact===null||likelihood<1||likelihood>5||impact<1||impact>5)return "Belum tersedia";const score=likelihood*impact;return riskThresholds.find(x=>score>=x.min&&score<=x.max)?.level??"Belum tersedia"}
export function alertStatus(level:string,validUntil:string|null,cutoff:string){if(!validUntil||!/^\d{4}-\d{2}-\d{2}$/.test(validUntil)||!/^\d{4}-\d{2}-\d{2}$/.test(cutoff))return "Belum tersedia";if(validUntil<cutoff)return "Selesai/Kedaluwarsa";return ({Rendah:"Normal",Sedang:"Waspada",Tinggi:"Siaga",Kritis:"Kritis"} as Record<string,string>)[level]??"Belum tersedia"}
export function riskRecommendations(record:{risk_type:string;consecutive_dry_days:number|null;water_availability_percent:number|null;rainfall_mm:number|null;affected_area_ha:number|null;notes:string}){const items:string[]=[];if(record.risk_type==="Kekeringan"||record.risk_type==="Ketersediaan air"){if(record.water_availability_percent!==null)items.push("Periksa ketersediaan air");items.push("Prioritaskan verifikasi jaringan irigasi","Koordinasikan jadwal distribusi air")}else if(record.risk_type==="Banjir/genangan"){if(record.affected_area_ha!==null)items.push("Verifikasi area terdampak");items.push("Periksa saluran drainase","Koordinasikan pemantauan lapangan")}else if(record.risk_type==="Hama dan penyakit"&&record.notes.includes("Observasi"))items.push("Verifikasi observasi petugas","Lakukan identifikasi organisme pengganggu");else items.push("Verifikasi indikator pendukung dan kondisi lapangan");return items}
